docker:
  - image: circleci/ruby:2.5-node-browsers
    environment:
      DB: postgresql
      PGHOST: 127.0.0.1
      PGUSER: root
      RAILS_ENV: test
  - image: circleci/postgres:latest-ram
    environment:
      POSTGRES_USER: root
