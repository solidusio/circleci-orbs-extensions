docker:
  - image: cimg/ruby:2.7-browsers
    environment:
      DB: postgresql
      PGHOST: 127.0.0.1
      PGUSER: postgres
      RAILS_ENV: test
  - image: cimg/postgres:14.2
    environment:
      POSTGRES_USER: postgres
