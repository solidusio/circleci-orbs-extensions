description: >
  Test latest release of Solidus

parameters:
  working_directory:
    type: string
    default: "~/project"

steps:
  - test-branch:
      branch: v4.5
      rails_version: "~> 7.2"
      working_directory: <<parameters.working_directory>>
