description: >
  Test the edge version of Solidus

parameters:
  working_directory:
    type: string
    default: "~/project"

steps:
  - test-branch:
      branch: main
      rails_version: "~> 8.0"
      working_directory: <<parameters.working_directory>>
